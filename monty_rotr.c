#include "monty.h"

/**
 * monty_rotr - rotates the stack to the bottom.
 * Usage: rotr
 * The last element of the stack becomes the top element of the stack
 * rotr never fails
 * Return: void
 */
void monty_rotr(stack_t **stack, u_int lineno)
{
	printf("monty_rotr called on line %u\n", lineno);
	(void)stack;
}